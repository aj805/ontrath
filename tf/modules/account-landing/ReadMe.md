# Account Landing Terraform Module

For shared resources across multiple environments in one AWS Account. For example an ECR Respository for a container image may be used across multiple deployment environments such as dev, qa, review, staging, etc. 